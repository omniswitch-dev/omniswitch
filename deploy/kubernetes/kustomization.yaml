apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - secret.example.yaml
  - configmap.yaml
  - redis.yaml
  - deployment.yaml
  - service.yaml

images:
  - name: ghcr.io/omniswitch-dev/omniswitch
    newTag: latest
